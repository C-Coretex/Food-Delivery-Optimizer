<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver
                            https://timefold.ai/xsd/solver/solver.xsd">

    <!-- Debug-friendly, catches mistakes early -->
    <!-- <environmentMode>FULL_ASSERT</environmentMode> -->
    <environmentMode>NO_ASSERT</environmentMode>

    <!-- MODEL -->
    <solutionClass>fdo.domain.DeliverySolution</solutionClass>
    <entityClass>fdo.domain.CourierShift</entityClass>
    <entityClass>fdo.domain.Visit</entityClass>

    <!-- SCORING -->
    <scoreDirectorFactory>
        <constraintProviderClass>
            fdo.solver.DeliveryConstraintProvider
        </constraintProviderClass>
    </scoreDirectorFactory>

    <!-- SOLVER -->
    <termination>
         <diminishedReturns />
    </termination>

    <constructionHeuristic />

    <localSearch>
        <termination>
            <secondsSpentLimit>20</secondsSpentLimit>
        </termination>
        <unionMoveSelector>
            <listChangeMoveSelector />
            <listSwapMoveSelector />
            <subListChangeMoveSelector>
                <maximumSubListSize>10</maximumSubListSize>
            </subListChangeMoveSelector>
        </unionMoveSelector>
        <acceptor>
            <lateAcceptanceSize>400</lateAcceptanceSize>
        </acceptor>
        <forager>
            <acceptedCountLimit>100</acceptedCountLimit>
        </forager>
    </localSearch>

    <localSearch>
        <unionMoveSelector>
            <listChangeMoveSelector />
            <listSwapMoveSelector />
            <subListChangeMoveSelector>
                <maximumSubListSize>10</maximumSubListSize>
            </subListChangeMoveSelector>
        </unionMoveSelector>
        <acceptor>
            <simulatedAnnealingStartingTemperature>4hard/500soft</simulatedAnnealingStartingTemperature>
        </acceptor>
        <forager>
            <acceptedCountLimit>100</acceptedCountLimit>
        </forager>
    </localSearch>


</solver>